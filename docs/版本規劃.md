







# 0.1.0
1. 專案管理：新建/開啟專案 [done]
2. 素材管理：基於檔案系統，設定角色、背景、音效 [done]
3. 模板系統：基於檔案系統，引入資料包（角色/背景/BGM/SE，等於在FileSytem丟進一個符合格式的目錄。 [done]
4. 劇本自動補全：ScriptEditor自動提供指令/選項/資源的候選。 [done]
5. 劇本格式文件與實作一致 [done]


# 0.1.1
1. 輸出影片到working directory/exports
2. 輸出影片可以指令名稱/編碼方式/最基本的渲染設定(參數直接從UI拿)
3. 最小劇本資料持續：掃描 working directory/scripts/*.waku，先固定打開第一個，儲存時寫回同一個檔案

# 0.1.2

2. Render 錯誤訊息可讀化
5. 編譯時錯誤提示：劇本錯誤，顯示錯誤視窗
3. 素材缺失錯誤：找不到角色/背景/音效時指出名稱
4. 範例專案或模板包整理

# 0.2.0：舞台系統
目標：把演出層整理成紙劇場式 Stage，使用 Node3D 取得 camera / renderer 彈性，但劇本與高層 API 維持 anchor-first 的舞台語意。

1. StageViewport / StageActor / StageAnchor 命名與責任整理
2. Actor 從 Control 遷移到 Node3D，劇本位置以 StageAnchor 名稱解析
3. ImageActorRenderer：以 Sprite3D 顯示現有 2D 角色素材
4. Actor 動畫介面整理：FadeIn / Teleport / MoveTo / MakeExpression / TakePose
5. DialoguePanel 保留為 UI overlay，不併入 3D 舞台
6. 詳細概念見 [舞台系統](舞台系統.md)



# 0.3.0：角色設定
1. actor meta.json 讀寫
2. default_expression / default_body
3. voice 設定
4. ResourceManager 掃描時補預設 meta.json
5. DialogueInstruction 使用 actor voice



# 0.4.0 劇本資料管理
1. 專案可以開啟、建立、刪除 scripts/*.waku 劇本
2. ProjectData 記錄目前開啟的劇本
3. App 開啟專案時恢復上次編輯的劇本
4. 依賴 ProjectData schemaVersion / migration

# 0.4.1 影片設定
0. ProjectData 導入 schemaVersion / migration
1. ProjectData 加入 video profile
- resolution
- fps
- oreientaion(landscape / portrait)
- video layout(對話框位置/樣式設定)
2. APP行為要反映ProjectData


# 0.5.0 編輯器即時錯誤提示
1. Script diagnostics model
2. parser 不直接 throw，回傳錯誤列表
3. editor 行號/區段標紅
4. 錯誤訊息支援資源缺失、語法錯誤、參數錯誤



# 0.6.0 多語系劇本
1. keyword alias / language pack
2. 同一 instruction schema 支援多語 keyword
3. completion 依語言顯示
4. 舊中文劇本相容


# 0.7.0  TTS source abstraction
SystemSpeechTTS / Sherpa / 未來 ONNX 統一成介面
# 0.8.0  內建 ONNX TTS source
kokoro, piper



# Some Other
1. Visual Completion / 素材 picker
   - 表情候選顯示縮圖
   - 動作候選顯示角色 pose
   - 背景候選顯示背景縮圖
   - 音效候選可試聽
2. CameraWork 模板(比如：抖動攝影機、Zoom 到角色、frame 角色群、BodyShot、UpperBodyShot、CloseUp)
3. ModelActorRenderer：3D 模型作為可動紙片人來源
4. Live2DActorRenderer：Live2D 作為可動紙片人來源
5. 簡單特效/濾鏡/畫面效果
6. Keyframe 編輯(比如：動畫表情)
